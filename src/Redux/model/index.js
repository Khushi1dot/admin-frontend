import authObj from "./auth/index";
import postsObj from "./posts/index";
import dashboardObj from "./dashboard/index";

const models = {
  auth: authObj,
  posts: postsObj,
   dashboard: dashboardObj,
};

export default models;
